<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="page-shell">
      <section class="hero-card hero-card-home hero-card-projects blog-detail-hero">
        <%- include("../components/navbar", { active: typeof active !== "undefined" ? active : "projects" }) %>

        <div class="blog-detail-hero-body">
        <main>
        <%- include("../components/blog-page", { blogPage }) %>
        </main>
        </div>
      </section>

   
    </div>
  </body>
</html>
